
paccurate is default provider for Kibo

our clients should be able to use dev center to setup or specify a different cartonization provider

we will have a microservice for integrating with paccurate and use extensibility

also need extensibility for the APIs fulfiller ui calls, so tht product attributes can be passed ot the cartonization provider for example to leverage provider based rules, etc

